# CMake script to help improve cross-platform usage of pam_lua
# Partly an experiment... a lua rock will be soon ... if it can handle needs
cmake_minimum_required(VERSION 2.6)
project(cryptoface C)

subdirs(cryptoface)
